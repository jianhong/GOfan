% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sunburstGO.R
\name{sunburstGO}
\alias{sunburstGO}
\title{Sunburst plot for enriched GO term}
\usage{
sunburstGO(
  df,
  org,
  g,
  termID = "ID",
  fill = "qvalue",
  sub_rect = NULL,
  GO_annotation_level_cutoff = 4,
  filterNodesByEdgeNumber = 2,
  mustkeep = c(),
  onlyKeep = c(),
  fillNAby0 = TRUE,
  onto = c("BP", "CC", "MF"),
  plotBy = c("plotly", "ggplot2"),
  ...
)
}
\arguments{
\item{df}{A data frame with enriched GO terms}

\item{org}{An OrgDb object}

\item{g}{An igraph graph. Output of \link{getGraph}.}

\item{termID}{Column name in df which store the GO IDs}

\item{fill}{Column name in df used to set the fill colors}

\item{sub_rect}{Column name in df used to set the area of a proportional
sub-rectangle, which represent a share of the whole. The values should be
a number in the range from 0 to 1. If it is a count number, it will be convert
to a proportion by divided the total number of features in the term.
Otherwise, will simply re-scale to .}

\item{GO_annotation_level_cutoff}{The cutoff of the GO annotation levels}

\item{filterNodesByEdgeNumber}{Filter the sub graphs by the edge numbers.}

\item{mustkeep}{The GO terms must be kept.}

\item{onlyKeep}{Only keep branches with give GO terms.}

\item{fillNAby0}{Fill the NA values by 0 or not for the color column.}

\item{onto}{The ontology category of the GO IDs}

\item{plotBy}{plot tools, plotly or ggplot2.}

\item{...}{parameter passed to \link{ggSunburst}.}
}
\value{
A plot handle
}
\description{
Sunburst plot for enriched GO term
}
\examples{
library(org.Dr.eg.db)
df <- data.frame(
    ID = c("GO:0007267", "GO:0099536", "GO:0099537", "GO:0098916"),
    qvalue = -10 * log10(runif(4, max = 0.05))
)
sunburstGO(df, org.Dr.eg.db)
}
